import {icon} from '../../src/icons.mjs';
import {button,badge,escapeHTML as e} from '../../src/components.mjs';
import {CATEGORIES,titleFor,propertyFor,contrast,contrastGrade,filterTokens} from './tokens.mjs';
export {e};
const arrow=()=>icon('arrow');
export const pageInfo={overview:['Overview','A shared language. A better World.'],colors:['Color','A palette with a purpose.'],typography:['Typography','Make every word feel considered.'],spacing:['Spacing','Room to breathe. Rhythm to build.'],radii:['Radii','A softer side to the system.'],elevation:['Elevation','Make the hierarchy tangible.'],motion:['Motion','Move with intention.'],layout:['Layout','Start with a strong structure.'],components:['Components','Small parts. Complete experiences.'],accessibility:['Accessibility','Great design includes everyone.'],source:['Source','One file. One source of truth.'],tokens:['All tokens','Find your building blocks.']};
const navItem=(id,label,ico,page,count='')=>`<a class="ds-nav-item ${page===id?'is-active':''}" href="#/${id}" ${page===id?'aria-current="page"':''}>${icon(ico)}<span>${label}</span>${count!==''?`<small>${count}</small>`:''}</a>`;
export function shell(store,state){return `<aside class="ds-sidebar" aria-label="Design system navigation"><a class="ds-wordmark" href="#/overview"><svg width="32" height="29" viewBox="0 0 40 32" aria-hidden="true"><path d="m3 5 8 22L20 5l9 22 8-22" fill="none" stroke="currentColor" stroke-width="7" stroke-linecap="round" stroke-linejoin="round"/></svg><span>World Agent<small>DESIGN SYSTEM</small></span></a><div class="ds-workspace"><span class="ds-workspace-icon">W</span><span>World Agent<small>Product foundations</small></span><span class="ds-version">1.0</span></div><nav><div class="ds-nav-block">${navItem('overview','Overview','home',state.page)}<p class="ds-nav-label">FOUNDATIONS</p>${CATEGORIES.map(c=>navItem(c.id,c.label,c.icon,state.page,store.tokens.filter(t=>t.category===c.id).length)).join('')}<p class="ds-nav-label">BUILD & EXPLORE</p>${navItem('components','Components','flask',state.page)}${navItem('accessibility','Accessibility','shield',state.page)}${navItem('source','Source code','report',state.page)}</div></nav><div class="ds-sidebar-footer"><span class="ds-live-dot"></span><div>Source connected<code>src/tokens.css</code></div>${icon('check')}</div></aside>
<div class="ds-workspace-shell"><header class="ds-topbar"><div class="ds-breadcrumb"><span>World Agent</span><span>/</span><strong>Design System</strong></div><label class="ds-search">${icon('search')}<input id="token-search" type="search" placeholder="Search tokens…" aria-label="Search all tokens" value="${e(state.query)}"><kbd>⌘ K</kbd></label><div class="ds-topbar-actions"><button class="ds-icon-button" data-action="toggle-inspector" aria-label="Toggle token inspector" title="Toggle token inspector">${icon('settings')}</button><button class="ds-button ds-button--dark" data-action="export-css">${icon('download')}<span>Export CSS</span></button></div></header><div class="ds-layout"><main class="ds-main" id="main-content" tabindex="-1"><div id="content"></div><footer class="ds-main-footer"><span>World Agent Design System <span class="ds-footer-dot">·</span> v1.0 tokens</span><span>Built from the source. Not a screenshot.</span></footer></main><aside class="ds-inspector" id="inspector" aria-label="Token inspector"></aside></div></div><button class="ds-inspector-backdrop" data-action="close-inspector" aria-label="Close token inspector"></button>`;}
export function pageHeader(page,store,state){const [label,title]=pageInfo[page]||pageInfo.overview;return `<div class="ds-page-heading"><div><div class="ds-eyebrow"><span class="ds-live-dot"></span>${page==='overview'?'THE WORLD AGENT DESIGN LANGUAGE':e(label.toUpperCase())}<span class="ds-heading-version">v1.0</span></div><h1>${title}</h1><p>${page==='overview'?'The foundations behind every thoughtful interaction.<br>Explore the tokens. See the components. Make it yours.':page==='components'?'Real, reusable components built with the very same tokens.':page==='source'?'This is the stylesheet the explorer reads—not a manually maintained catalog.':page==='accessibility'?'Check specific color pairs. Understand the limits. Design with confidence.':'Select a token to inspect its value, preview a change, or copy its CSS reference.'}</p></div>${page==='overview'?'<div class="ds-hero-art" aria-hidden="true"><div class="ds-art-grid"></div><div class="ds-art-card art-1"></div><div class="ds-art-card art-2"></div><div class="ds-art-card art-3"><span>W</span></div><i class="ds-art-dot"></i><span class="ds-art-label">DESIGNED TO CONNECT</span></div>':''}</div>${store.changedCount?`<div class="ds-draft-banner">${icon('info')}<span><strong>${store.changedCount} preview ${store.changedCount===1?'override':'overrides'} active.</strong> The source file is unchanged.</span><button data-action="reset-all">Reset all</button></div>`:''}`;}
function tokenArt(t){const n=t.name;switch(t.category){case 'colors':return `<div class="ds-token-art ds-color-art" style="background:var(${n})"><span style="color:${(contrast(t.current,'#ffffff')??0)>=4.5?'#ffffff':'#070d2d'}">Aa</span></div>`;case 'typography':return `<div class="ds-token-art ds-type-art" style="${n.startsWith('--font-')?'font-family':n.startsWith('--weight-')?'font-weight':'font-size'}:var(${n})">Aa<span>字</span></div>`;case 'spacing':return `<div class="ds-token-art ds-space-art"><span></span><i style="width:var(${n})"></i><span></span></div>`;case 'radii':return `<div class="ds-token-art ds-radius-art"><i style="border-radius:var(${n})"></i></div>`;case 'elevation':return `<div class="ds-token-art ds-elevation-art"><i style="box-shadow:var(${n})"></i></div>`;case 'motion':return `<div class="ds-token-art ds-motion-art"><i></i><span>${n.startsWith('--duration-')?e(t.current):icon('chart')}</span></div>`;default:return `<div class="ds-token-art ds-layout-art"><i style="width:min(var(${n}),65%)"></i><span></span></div>`;}}
export function tokenCard(token,store,state){const t=store.get(token.name);return `<article class="ds-token-card ${state.selected===t.name?'is-selected':''} ${t.changed?'is-changed':''}" data-token-card="${t.name}"><button class="ds-token-open" data-action="inspect" data-name="${t.name}" aria-label="Inspect ${t.name}" aria-pressed="${state.selected===t.name}"><div data-token-scope>${tokenArt(t)}</div><div class="ds-token-meta"><strong>${e(titleFor(t.name))}${t.changed?'<i class="ds-changed-dot" title="Preview override active"></i>':''}</strong><code>${e(t.name)}</code></div></button><div class="ds-token-bottom"><code title="${e(t.current)}">${e(t.current)}</code><button class="ds-copy-button" data-action="copy-reference" data-name="${t.name}" aria-label="Copy CSS reference for ${t.name}">${icon('report')}</button></div></article>`;}
const sectionHeading=(title,link='',text='View all')=>`<div class="ds-section-heading"><h2>${title}</h2>${link?`<a href="#/${link}">${text} ${arrow()}</a>`:''}</div>`;
export function overview(store,state){const pick=names=>names.map(n=>store.tokens.find(t=>t.name===n)).filter(Boolean);return `${pageHeader('overview',store,state)}<div class="ds-stats"><div><strong>${store.tokens.length}<small>tokens</small></strong><span>One consistent source</span></div><div><strong>${CATEGORIES.length}<small>foundations</small></strong><span>Every detail, considered</span></div><div><strong>CSS<small>native</small></strong><span>No runtime dependencies</span></div></div><section class="ds-section">${sectionHeading('The signature palette','colors','Explore color')}<div class="ds-token-grid ds-signature-grid">${pick(['--color-brand','--color-ink','--color-surface-soft','--color-success','--color-error']).map(t=>tokenCard(t,store,state)).join('')}</div></section><div class="ds-foundation-pair"><section class="ds-foundation-preview">${sectionHeading('A voice that feels human','typography','Type scale')}<button class="ds-type-specimen" data-action="inspect" data-name="--font-sans" data-token-scope><span>Aa <span>字</span></span><strong>Clarity in every character.</strong><p>更好的洞察，更美好的明天。</p><code>--font-sans · System fallback stack</code></button></section><section class="ds-foundation-preview">${sectionHeading('Space creates rhythm','spacing','Spacing scale')}<div class="ds-space-specimen" data-token-scope>${pick(['--space-1','--space-2','--space-3','--space-4','--space-6','--space-8']).map(t=>`<button data-action="inspect" data-name="${t.name}" aria-label="Inspect ${t.name}"><span><i style="height:var(${t.name})"></i></span><strong>${e(store.get(t.name).current.replace('px',''))}</strong><code>${t.name.replace('--space-','')}</code></button>`).join('')}</div><div class="ds-specimen-note">A deliberate scale. From 4px to 48px.</div></section></div><section class="ds-section">${sectionHeading('One token. Every touchpoint.','components','Explore components')}<div class="ds-connected-preview"><div class="ds-connected-copy"><span class="ds-kicker">LIVE COMPONENT PREVIEW</span><h3>Change a value. <br>See the whole picture.</h3><p>Try editing <button data-action="inspect" data-name="--color-brand"><code>--color-brand</code></button> in the inspector. The same token powers each component.</p><span class="ds-preview-badge">${icon('flask')} Browser-only playground</span></div><div class="ds-mini-product" data-token-scope><div class="ds-mini-top"><span class="ds-mini-logo">W</span><strong>实验中心</strong>${badge('Ready','success')}</div><h3>新品上市模拟</h3><p>在数字世界中，验证下一次市场决策。</p><div class="ds-mini-schemes" role="group" aria-label="Preview scenario">${['A','B','C'].map(s=>`<button data-action="scenario" data-id="${s}" class="${state.scenario===s?'is-active':''}"><span>方案 ${s}</span><strong>¥${s==='A'?6:8}</strong></button>`).join('')}</div><div class="ds-mini-bottom"><span><small>演示销量</small><strong>${{A:'1,260,000',B:'1,420,000',C:'1,310,000'}[state.scenario]}</strong></span>${button('开始仿真',{action:'preview-run',iconName:'arrow'})}</div></div></div></section><div class="ds-source-note">${icon('report')}<span>Every value on this page is read from <a href="#/source">src/tokens.css</a>.</span><a href="#/source">View source ${arrow()}</a></div>`;}
function colorsContent(tokens,store,state){const groups=[['Brand & interaction',t=>/--color-brand/.test(t.name)],['Neutrals & surfaces',t=>/--color-(ink|text|muted|surface|input|border|frame)/.test(t.name)],['Status & feedback',t=>/--color-(success|error|warning)/.test(t.name)],['Data visualization',t=>/--color-(chart|purple)/.test(t.name)]];return groups.map(([label,predicate])=>{const list=tokens.filter(predicate);return list.length?`<section class="ds-section">${sectionHeading(label)}<div class="ds-token-grid">${list.map(t=>tokenCard(t,store,state)).join('')}</div></section>`:'';}).join('');}
export function catalog(store,state){const category=state.page==='tokens'?'all':state.page;const tokens=filterTokens(store.tokens,state.query,category);const description=category==='all'?'tokens':CATEGORIES.find(c=>c.id===category)?.label.toLowerCase()+' tokens';const toolbar=`<div class="ds-catalog-toolbar"><span><strong>${tokens.length}</strong> ${e(description)}${state.query?` matching <strong>“${e(state.query)}”</strong>`:''}</span><div class="ds-view-switch" role="group" aria-label="Token view"><button data-action="view" data-view="grid" aria-label="Grid view" aria-pressed="${state.view==='grid'}" class="${state.view==='grid'?'is-active':''}">${icon('cube')}</button><button data-action="view" data-view="table" aria-label="Table view" aria-pressed="${state.view==='table'}" class="${state.view==='table'?'is-active':''}">${icon('menu')}</button></div></div>`;
 let content='';if(!tokens.length)content=`<div class="ds-empty">${icon('search')}<h2>No tokens found.</h2><p>Try “brand”, “16px”, or “shadow”.</p><button class="ds-button" data-action="clear-search">Clear search</button></div>`;else if(state.view==='table')content=`<div class="ds-table-scroll"><table class="ds-table"><thead><tr><th>Token</th><th>Value</th><th>Category</th><th><span class="ds-sr-only">Inspect</span></th></tr></thead><tbody>${tokens.map(t=>{const live=store.get(t.name);return `<tr><td><button data-action="inspect" data-name="${t.name}">${t.category==='colors'?`<i class="ds-small-swatch" data-token-scope style="background:var(${t.name})"></i>`:''}<code>${t.name}</code></button></td><td><code>${e(live.current)}</code>${live.changed?'<i class="ds-changed-dot"></i>':''}</td><td>${t.category}</td><td><button class="ds-icon-button" data-action="inspect" data-name="${t.name}" aria-label="Inspect ${t.name}">${arrow()}</button></td></tr>`;}).join('')}</tbody></table></div>`;else if(category==='colors')content=colorsContent(tokens,store,state);else if(category==='typography')content=`<div class="ds-typography-head" data-token-scope><span>Aa 字</span><h2>More Human Insights.</h2><p>更好的洞察，更美好的明天。</p><code>Inter → system UI → Chinese fallback → sans-serif</code></div><div class="ds-token-grid">${tokens.map(t=>tokenCard(t,store,state)).join('')}</div>`;else content=`<div class="ds-token-grid ${category==='elevation'?'ds-elevation-grid':''}">${tokens.map(t=>tokenCard(t,store,state)).join('')}</div>`;
 return `${pageHeader(state.page,store,state)}${toolbar}${content}${category==='motion'?`<section class="ds-motion-playground ds-section">${sectionHeading('Feel the timing')}<p>The same distance. Different durations. Honors reduced-motion preferences.</p><div data-token-scope>${tokens.filter(t=>t.name.startsWith('--duration-')).map(t=>`<div class="ds-motion-track"><code>${t.name}</code><div><i style="transition-duration:var(${t.name});transition-timing-function:var(--ease-standard)"></i></div><span>${e(store.get(t.name).current)}</span></div>`).join('')}</div><button class="ds-button ds-button--dark" data-action="play-motion">${icon('play')} Replay motion</button></section>`:''}`;
}
export function components(store,state){return `${pageHeader('components',store,state)}<div class="ds-component-tabs" role="tablist" aria-label="Component gallery">${['Buttons','Forms','Feedback','Cards'].map(t=>`<button role="tab" data-action="component-tab" data-tab="${t}" aria-selected="${state.component===t}" aria-controls="component-panel" class="${state.component===t?'is-active':''}">${t}</button>`).join('')}</div><div id="component-panel" role="tabpanel" aria-label="${state.component}">${state.component==='Buttons'?`<section class="ds-component-block">${sectionHeading('Button variants')}<p>One main action. Supporting actions stay visually quieter.</p><div class="ds-component-stage" data-token-scope>${['primary','secondary','outline','ghost'].map(v=>`<div>${button(v==='primary'?'Start simulation':titleFor(v),{variant:v,action:'sample-action',iconName:v==='primary'?'arrow':''})}<code>${v}</code></div>`).join('')}</div><pre class="ds-code"><code>${e("button('Start simulation', { variant: 'primary', iconName: 'arrow' })")}</code></pre></section><section class="ds-component-block">${sectionHeading('Every state, intentionally designed')}<div class="ds-state-grid" data-token-scope>${[['Default',''],['Hover','hover'],['Active','active'],['Focus','focus'],['Disabled','disabled'],['Loading','loading']].map(([label,st])=>`<div class="wa-state-${st}">${button(st==='loading'?'Running':'Continue',{disabled:st==='disabled',loading:st==='loading',action:'sample-action'})}<span>${label}</span></div>`).join('')}</div><div class="ds-component-note">${icon('info')} Hover, active, and focus are pinned here for comparison; live buttons respond normally.</div></section><section class="ds-component-block">${sectionHeading('Right-sized for the moment')}<div class="ds-component-stage" data-token-scope>${['sm','md','lg'].map(size=>`<div>${button('Continue',{size,action:'sample-action'})}<code>${size} · ${size==='sm'?32:size==='md'?40:48}px</code></div>`).join('')}</div></section><div class="ds-doc-note"><h3>Behavior & accessibility</h3><p>Native buttons respond to Enter and Space. Disabled and loading buttons cannot submit actions. Focus uses <button data-action="inspect" data-name="--focus-ring"><code>--focus-ring</code></button>. Respect reduced motion; use one primary action per decision.</p></div>`:state.component==='Forms'?`<section class="ds-component-block">${sectionHeading('Form fields')}<p>Persistent labels. Useful helper text. Errors that explain the next step.</p><div class="ds-form-stage" data-token-scope><label class="wa-field"><span>Product name</span><input placeholder="White peach sparkling water"><small>Clear, concise, and recognizable.</small></label><label class="wa-field"><span>Category</span><select><option>Beverage</option><option>Beauty</option><option>Household</option></select></label><label class="wa-field"><span>Work email</span><input type="email" value="not-an-email" aria-invalid="true" aria-describedby="sample-error"><small class="wa-error" id="sample-error">Enter an email address, like name@company.com.</small></label><label class="wa-field"><span>Workspace ID</span><input value="wa-demo-001" disabled><small>Disabled values cannot be edited.</small></label></div></section><section class="ds-component-block">${sectionHeading('A small switch, a clear choice')}<div class="ds-switch-stage" data-token-scope><div><strong>Auto-save experiments</strong><p>Interactive example only. This does not change draft persistence.</p></div><button class="wa-switch" type="button" role="switch" aria-checked="${state.autoSave}" aria-label="Auto-save example" data-action="switch"></button></div></section><div class="ds-doc-note"><h3>Keyboard & validation</h3><p>Tab moves between controls. Space toggles the switch. Invalid fields use aria-invalid and an associated error message. Labels remain visible after typing.</p></div>`:state.component==='Feedback'?`<section class="ds-component-block">${sectionHeading('Status in a glance')}<div class="ds-component-stage" data-token-scope>${[['Draft','brand'],['Completed','success'],['Needs review','warning'],['Failed','error']].map(([l,t])=>`<div>${badge(l,t)}<code>${t}</code></div>`).join('')}</div></section><section class="ds-component-block">${sectionHeading('A response, not an interruption')}<div class="ds-feedback-stage" data-token-scope><div class="ds-feedback-demo">${icon('check')}<span><strong>Your experiment is saved.</strong><small>A toast confirms the action without taking focus.</small></span></div>${button('Show toast',{variant:'secondary',action:'sample-toast'})}${button('Open dialog',{action:'sample-modal'})}</div></section><div class="ds-doc-note"><h3>Focus & announcements</h3><p>Toasts use a polite live region. The dialog traps focus, supports Escape, and restores focus to its trigger. Color is always accompanied by explanatory text.</p></div>`:`<section class="ds-component-block">${sectionHeading('Data with a little context')}<div class="ds-card-stage" data-token-scope><article class="wa-card"><span class="ds-card-label">Estimated sales · demo</span><h3 class="ds-card-metric">1,420,000 <small>bottles</small></h3>${badge('↑ 18% vs baseline','success')}<p>Use context to make a number meaningful.</p></article><article class="wa-card"><span class="ds-card-label">Consumer world</span><h3>100,000 Agent profiles</h3><p>Illustrative sample data, not a verified product capability.</p>${button('Explore profiles',{variant:'outline',action:'sample-action'})}</article></div></section><div class="ds-doc-note"><h3>Token recipe</h3><p>A card combines <code>--color-surface</code>, <code>--color-border</code>, <code>--radius-lg</code>, <code>--space-5</code>, and <code>--shadow-card</code>. Change any of these in the inspector to see it respond.</p></div>`}</div>`;}
export const CONTRAST_PAIRS=[['Primary button','--color-surface','--color-brand'],['Body text','--color-text','--color-surface'],['Muted text','--color-muted','--color-surface'],['Success message','--color-success','--color-success-soft'],['Error message','--color-error','--color-error-soft'],['Warning message','--color-warning','--color-warning-soft'],['Outline action','--color-brand','--color-surface-soft']];
export function accessibility(store,state){const pairs=CONTRAST_PAIRS.map(([label,fg,bg])=>({label,fg,bg,ratio:contrast(store.get(fg).current,store.get(bg).current)}));return `${pageHeader('accessibility',store,state)}<div class="ds-audit-summary"><div>${icon('shield')}<strong>${pairs.filter(p=>p.ratio!==null&&p.ratio>=4.5).length} / ${pairs.length}</strong><span>sample pairs pass normal-text AA</span></div><p>Contrast checks, not a compliance score.<br>Results depend on the exact foreground and background pair.</p></div><div class="ds-table-scroll"><table class="ds-table ds-contrast-table"><thead><tr><th>Usage</th><th>Foreground / background</th><th>Ratio</th><th>Normal text</th><th>Large text</th></tr></thead><tbody>${pairs.map(p=>`<tr><td><span class="ds-contrast-sample" data-token-scope style="color:var(${p.fg});background:var(${p.bg})">Aa</span><strong>${p.label}</strong></td><td><button data-action="inspect" data-name="${p.fg}"><code>${p.fg}</code></button><small>${p.bg}</small></td><td>${p.ratio===null?'—':p.ratio.toFixed(2)+':1'}</td><td><span class="ds-audit-badge ${p.ratio!==null&&p.ratio>=4.5?'pass':'review'}">${p.ratio===null?'Not evaluated':p.ratio>=4.5?'Pass':'Review'}</span></td><td>${p.ratio===null?'—':p.ratio>=3?'Pass':'Review'}</td></tr>`).join('')}</tbody></table></div><div class="ds-doc-note"><h3>How these checks work</h3><p>Opaque sRGB hex colors only. Normal text requires at least 4.5:1; large text requires 3:1. Decisions use the unrounded ratio. Gradients, transparency, imagery, and full-page accessibility are not evaluated. These checks do not certify WCAG compliance.</p><a href="https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html" target="_blank" rel="noopener noreferrer">WCAG 2.2 contrast guidance ${arrow()}</a></div>`;}
export function sourceView(store,state){return `${pageHeader('source',store,state)}<div class="ds-source-header"><div>${icon('report')}<code>src/tokens.css</code><span>${store.tokens.length} declarations</span></div><a class="ds-button" href="../src/tokens.css" target="_blank" rel="noopener">Open raw file ${icon('expand')}</a></div><pre class="ds-source-code"><code>${e(store.source)}</code></pre><div class="ds-doc-note"><h3>Source versus preview</h3><p>The source shown above stays unchanged while you experiment. Drafts are local to this browser and scoped to component previews. Export CSS to save a proposed token file; copy it into <code>src/tokens.css</code> and commit it when you are ready.</p><div class="ds-source-actions"><button class="ds-button ds-button--dark" data-action="export-css">${icon('download')} Export CSS</button><button class="ds-button" data-action="export-json">${icon('download')} Export JSON</button></div></div>`;}
export function inspector(store,state){const t=store.get(state.selected);if(!t)return '';const ratio=t.category==='colors'?contrast(t.current,'#ffffff'):null;return `<div class="ds-inspector-inner"><header><span>TOKEN INSPECTOR</span><button class="ds-icon-button" data-action="close-inspector" aria-label="Close inspector">${icon('close')}</button></header><div class="ds-inspector-category"><span>${CATEGORIES.find(c=>c.id===t.category)?.label}</span>${t.changed?'<span class="ds-draft-tag">Draft override</span>':'<span>Source value</span>'}</div><div class="ds-inspector-art" data-token-scope>${tokenArt(t)}</div><h2>${e(titleFor(t.name))}</h2><code class="ds-inspector-name">${t.name}</code><div class="ds-inspector-value"><code>${e(t.current)}</code><button class="ds-icon-button" data-action="copy-value" data-name="${t.name}" aria-label="Copy token value">${icon('report')}</button></div><div class="ds-inspector-divider"></div><form id="token-editor"><label for="draft-value">Preview value <span>Local draft</span></label><div class="ds-editor-input">${t.category==='colors'&&/^#[0-9a-f]{6}$/i.test(t.current)?`<input type="color" id="color-picker" aria-label="Choose preview color" value="${e(t.current)}">`:''}<input id="draft-value" name="value" value="${e(t.current)}" spellcheck="false" autocomplete="off" aria-describedby="editor-help editor-error"></div><p class="ds-editor-help" id="editor-help">Apply to the previews. Your CSS file stays unchanged.</p><p class="ds-editor-error" id="editor-error" role="alert"></p><button class="ds-button ds-button--brand" type="submit">Apply to previews ${arrow()}</button></form>${t.changed?`<button class="ds-reset-token" data-action="reset-token" data-name="${t.name}">Reset to ${e(t.value)}</button>`:''}<div class="ds-inspector-divider"></div><div class="ds-inspector-heading"><h3>CSS usage</h3><button class="ds-copy-button" data-action="copy-reference" data-name="${t.name}" aria-label="Copy CSS variable reference">${icon('report')}</button></div><pre class="ds-usage"><code>${propertyFor(t)==='color'?'color':propertyFor(t)}:<br>  var(${t.name});</code></pre>${t.category==='colors'?`<div class="ds-contrast-info"><h3>White text contrast</h3><strong>${ratio===null?'Not evaluated':ratio.toFixed(2)+':1'}</strong><span class="${ratio!==null&&ratio>=4.5?'is-pass':'is-review'}">${contrastGrade(ratio)}</span><p>${ratio===null?'This check supports opaque hex colors.':'Against #ffffff. Normal text AA needs 4.5:1.'}</p></div>`:''}<div class="ds-inspector-source">${icon('report')}<span>Single source of truth<br><a href="#/source">src/tokens.css ${arrow()}</a></span></div><div class="ds-inspector-tip">${icon('info')}<p>Preview changes are ${store.storageAvailable?'saved in this browser':'temporary; browser storage is unavailable'}. Export to use them in your project.</p></div></div>`;}
